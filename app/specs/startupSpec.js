import { expect } from 'chai';
import * as sinon from 'sinon';
import { the, should, when } from './../testing/specAliases';

the('startup module', () => {

  //require('./../bin/startup');

  before(() => {

  });

  after(() => {

  });
});