'use strict';

var buildPath = './build';
var tempPath = './temp';
var appPath = './app';
var packagePath = './package';

module.exports = {
   paths: {
      build: buildPath,
      temp: tempPath,
      app: appPath,
      package: packagePath
   },
   vendor: {
      styles: [],
      fonts: []
   }
};