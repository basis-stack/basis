const buildPath = './build';
const appPath = './app';
const packagePath = './package';

export default {
   paths: {
      build: buildPath,
      app: appPath,
      package: packagePath
   },
   vendor: {
      styles: [],
      fonts: []
   }
};