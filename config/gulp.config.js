const buildPath = './build';
const tempPath = './temp';
const appPath = './app';
const packagePath = './package';

export default {
   paths: {
      build: buildPath,
      temp: tempPath,
      app: appPath,
      package: packagePath
   },
   vendor: {
      styles: [],
      fonts: []
   }
};