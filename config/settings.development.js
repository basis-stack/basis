export default {

  // Development Environment Overrides (of settings.default)

  server: {
    webServerPort: 8080
  },

  deploy: {
    deployUser: '<DEV_USER>',
    deployHost: '<DEV_HOST>',
    deployDirectory: '<DEV_PATH>'
  },

  client: {
  }
};